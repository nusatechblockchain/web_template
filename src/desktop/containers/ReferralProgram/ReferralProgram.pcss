/* main content */

.content-wrapper {
    position: relative;
    margin-left: 223px;
    width: calc(100% - 223px);
    min-height: 100vh;
    padding-top: 70px;

    .referral-image {
        width: 120px;
    }
}

/* end main content */
