import * as React from 'react';

export const WalletWithdrawMobileScreen: React.FC = () => {
    return (
        <React.Fragment>
            <p>INI TEST WITHDRAW PAGE</p>
        </React.Fragment>
    );
};
